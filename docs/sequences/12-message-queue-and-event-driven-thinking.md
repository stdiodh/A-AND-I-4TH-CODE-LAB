# 12. 이벤트 기반 사고 확장

## 시퀀스 목적

이 시퀀스는 지금까지의 요청/응답 중심 구조 위에
`결과를 이벤트로 분리해 다른 흐름으로 넘기는 방식`을 처음 소개하는 단계입니다.

핵심은 고급 메시징 운영을 배우는 것이 아니라,
학생이 아래 질문에 답할 수 있게 만드는 것입니다.

- 왜 직접 호출만으로는 점점 불편해질 수 있을까
- 이벤트는 어떤 역할을 할까
- 메시지 큐는 발행자와 소비자를 어떻게 덜 묶어줄까

즉, 이번 시퀀스의 목표는 “완전한 MSA 구현”이 아니라
**이벤트 기반 사고를 입문 수준에서 한 번 확장해보는 것**입니다.

## 학생이 완성해야 하는 최종 이해

학생은 이 시퀀스를 마친 뒤 아래를 설명할 수 있어야 합니다.

1. 동기 호출과 비동기 전달의 차이
2. 이벤트가 무엇인지
3. 이벤트 발행과 소비 흐름
4. 메시지 큐가 왜 필요한지
5. 이벤트 기반 구조가 어떤 문제를 완화할 수 있는지
6. MSA가 무조건 답이 아니라는 점

## 시작 기준

이번 시퀀스는 `11-answer` 기준의 구조화된 Spring Boot 프로젝트 위에서 시작합니다.

이미 학생은 아래 감각을 가진 상태라고 가정합니다.

- CRUD / DTO / Validation / Exception Handling
- 인증 / 테스트 / 캐시 / 실시간 통신 / 배포 / 자동화
- 리팩토링을 통해 역할을 나누어 읽는 감각

이번 단계에서는 그 위에
`주문 생성 -> 이벤트 발행 -> 알림 소비`
예시 하나만 얹어서 흐름을 비교합니다.

## 레포 전략

- 사용 레포: `spring-boot-event-driven-lab`
- 안내 브랜치: `main`
- 학생 starter: `12-implementation`
- 정답 브랜치: `12-answer`

이번 시퀀스는 이전 레포를 계속 확장하지 않고,
이벤트와 메시지 큐라는 새 학습 주제를 분리한 별도 레포로 운영합니다.

## 학생 구현 순서

구현 순서는 반드시 아래 순서를 따릅니다.

1. 이벤트 DTO를 만듭니다.
2. 이벤트 발행 코드를 만듭니다.
3. 이벤트 소비 코드를 만듭니다.
4. 흐름을 실행해봅니다.
5. 동기/비동기 차이를 비교합니다.

문서와 TODO도 이 순서를 기준으로 설계합니다.

## TODO를 넣는 핵심 파일

- `OrderCreatedEvent.kt`
- `EventPublisherService.kt`
- `NotificationConsumer.kt`

보조 파일은 미리 제공할 수 있지만,
핵심 TODO는 위 세 파일에 집중합니다.

## 미리 제공하는 것

- RabbitMQ 실행 환경
- Producer / Consumer 기본 틀
- `OrderService`, `NotificationService`, `EventConfig`
- build 설정
- application 설정
- 주문 생성 API와 알림 조회 API
- 실행 가능한 starter 환경

학생은 보일러플레이트보다
발행/소비 흐름을 손으로 따라치는 데 집중해야 합니다.

## 구현 제약

- Kafka, RabbitMQ 고급 운영 지식을 깊게 넣지 않습니다.
- partition, consumer group, offset 심화를 넣지 않습니다.
- Saga, Outbox, CDC 같은 고급 패턴을 넣지 않습니다.
- MSA 전체 설계를 구현하지 않습니다.
- 예시는 `주문 생성 -> 알림 소비` 하나로 제한합니다.
- 메시지 브로커 운영 전체가 아니라 흐름 이해가 목적임을 분명히 합니다.

## 필수 산출물

`spring-boot-event-driven-lab`은 아래를 갖춘 상태여야 합니다.

- `README.md`
- `docs/theory.md`
- `docs/implementation.md`
- `docs/answer-guide.md`
- `docs/checklist.md`
- `docs/assets.md`
- `12-implementation` starter 코드
- `12-answer` 완성 코드
- `main` 안내 브랜치 문서

## 검증 기준

- `12-implementation`은 TODO가 있어도 실행 가능한 starter여야 합니다.
- `12-answer`는 완성 코드와 정답 문서가 일치해야 합니다.
- starter와 answer 둘 다 `./gradlew test`가 통과해야 합니다.
- 루트 허브에는 서브모듈과 상태표가 함께 반영되어야 합니다.

## 수업 메시지

이번 단계에서 학생이 꼭 가져가야 할 문장은 아래입니다.

- 이벤트는 “어떤 일이 일어났다”는 사실을 다른 흐름에 전달하는 방법입니다.
- 메시지 큐는 발행자와 소비자를 느슨하게 연결해줍니다.
- 동기 요청/응답만이 유일한 연결 방식은 아닙니다.
- 그렇다고 이벤트 기반 구조가 언제나 정답인 것도 아닙니다.
   * EDA
   * MSA 관점 소개
9. 자주 헷갈리는 포인트
10. 직접 말해보기
11. 복습 체크리스트
12. 오늘 꼭 기억할 것

주의:

* MQ, EDA, MSA를 한 덩어리로 깊게 설명하지 마세요.
* 코드와 반드시 연결하세요.
* 짧은 코드 블록을 사용하세요.
* 학생이 “아, 직접 호출만 하는 방식 말고 결과를 이벤트로 넘기는 흐름도 있구나”를 느끼게 써야 합니다.

---

## 12. implementation.md 작성 규칙

docs/implementation.md 는 아래 구조를 따르세요.

1. 제목
2. 오늘 학생이 완성할 최종 흐름
3. 학생이 직접 구현할 순서
4. TODO를 넣을 파일
5. 파일별 역할 설명
6. 단계별 구현 안내

   * Step 1. 이벤트 DTO 만들기
   * Step 2. 이벤트 발행 코드 만들기
   * Step 3. 이벤트 소비 코드 만들기
   * Step 4. 흐름 실행해보기
   * Step 5. 동기/비동기 차이 비교하기
7. 각 단계의 확인 포인트
8. 학생 체크 질문
9. 강사용 확인 포인트
10. 시퀀스 마무리 정리

주의:

* 결과만 적지 말고 손의 순서가 보여야 합니다.
* 단계별로 학생이 막히기 쉬운 부분을 짧게 힌트로 적으세요.
* 각 단계가 끝났을 때 무엇이 보이면 성공인지 써주세요.
* 마지막 시퀀스이므로 “전체 커리큘럼을 마치며” 관점도 짧게 정리하세요.

---

## 13. answer-guide.md 작성 규칙

docs/answer-guide.md 는 정답 문서입니다.

반드시 아래를 포함하세요.

* 각 파일의 최종 형태 설명
* OrderCreatedEvent 정답 코드
* EventPublisherService 정답 코드
* NotificationConsumer 정답 코드
* 주문 → 이벤트 발행 → 알림 소비 흐름 예시
* 동기 호출 버전과 이벤트 버전 비교 설명
* 학생이 자주 틀리는 포인트
* 왜 MQ가 필요한지 설명
* 왜 EDA가 시스템 확장 관점에서 의미가 있는지 설명
* 왜 MSA는 무조건 답이 아닌지 설명
* 커리큘럼 전체를 마무리하는 짧은 정리

정답 문서는 너무 장황하게 쓰지 말고,
강사가 빠르게 비교하고 학생도 복습하기 쉬운 형태로 만드세요.

---

## 14. starter 코드 파일 작성 규칙

starter 파일은 아래 성격을 가져야 합니다.

* 바로 읽고 수정할 수 있어야 함
* 핵심 부분은 비어 있거나 TODO로 남아 있어야 함
* TODO는 학생이 수업 시간 안에 채울 수 있어야 함
* TODO 없는 보일러플레이트는 제공
* 이벤트 흐름을 직접 확인할 수 있어야 함

필수 starter 파일 예시:

* `src/main/kotlin/.../event/OrderCreatedEvent.kt`
* `src/main/kotlin/.../service/EventPublisherService.kt`
* `src/main/kotlin/.../consumer/NotificationConsumer.kt`

필요하면 추가 가능:

* `OrderService.kt`
* `NotificationService.kt`
* `EventConfig.kt`

단, 범위를 과하게 넓히지 마세요.

---

## 15. checklist.md 작성 규칙

docs/checklist.md 에는 반드시 아래를 넣으세요.

### 학생 체크리스트

예:

* 동기와 비동기 차이를 설명할 수 있다.
* MQ가 왜 필요한지 말할 수 있다.
* EDA가 어떤 문제를 해결하는지 이해한다.
* MSA는 무조건 쓰는 것이 아니라는 점을 안다.

### 강사 / PPT 체크리스트

예:

* MQ, EDA, MSA를 한 덩어리로 설명하지 않는가
* 이 시퀀스의 목표가 완전 구현이 아니라 시야 확장임을 분명히 했는가
* 학생이 마지막에 큰 구조를 가볍게 이해하도록 설계했는가

또 아래도 포함하세요.

* 수업 전 준비 체크
* 수업 중 질문 체크
* 수업 후 복습 체크

---

## 16. assets.md 작성 규칙

docs/assets.md 에는 아래를 넣으세요.

* 제공 파일/설정 목록
* 각 항목의 목적
* 왜 학생이 직접 치지 않아도 되는지
* 수업에서 언제 쓰는지

예:

* 브로커 실행 환경: 인프라 설명보다 이벤트 흐름 이해에 집중시키기 위해 제공
* Producer/Consumer 틀: 학생이 핵심 발행/소비 흐름에 집중하게 하기 위해 제공
* 이벤트 DTO 예시: 구조를 빠르게 이해시키기 위해 제공

---

## 17. 절대 하지 말아야 할 것

* Kafka/RabbitMQ 고급 운영 지식을 넣지 마세요.
* Outbox, Saga, CDC 같은 고급 패턴을 넣지 마세요.
* MSA 전체 구현을 시도하지 마세요.
* 마지막 시퀀스라고 해서 범위를 과도하게 넓히지 마세요.
* 이번 요청에서는 12 시퀀스 범위를 벗어나지 마세요.

---

## 18. 출력 방식

최종 출력은 아래 순서로 작성하세요.

1. 생성할 파일 목록
2. 각 파일의 목적 요약
3. 추천 파일 구조
4. starter 파일 목록
5. 각 파일의 전체 마크다운/코드 초안

   * README.md
   * docs/theory.md
   * docs/implementation.md
   * docs/answer-guide.md
   * docs/checklist.md
   * docs/assets.md
   * starter 파일들

이번 작업은 반드시 **12. 이벤트 기반 사고 확장** 에만 한정합니다.
다른 시퀀스 내용은 추가하지 마세요.
