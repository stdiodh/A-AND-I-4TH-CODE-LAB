# A&I 백엔드 커리큘럼 구현 범위 계획서

## 문서 목적

이 문서는 A&I 백엔드 커리큘럼을 입문자 기준으로 운영할 때,
각 도메인별 구현 범위를 어떻게 잡을지 정리한 기준 문서입니다.

이 문서의 핵심 목표는 아래와 같습니다.

1. Codex가 실습 레포를 만들 때 구현 범위를 과하거나 부족하지 않게 잡을 수 있어야 한다.
2. 학생이 어떤 순서로 코드를 작성해야 하는지 한눈에 보이게 해야 한다.
3. 반복 설정과 무거운 부가 기능은 제공하고, 핵심 흐름만 직접 구현하게 해야 한다.
4. 강사가 수업이나 PPT를 준비할 때 이 문서의 체크리스트만으로 빠짐없이 준비할 수 있어야 한다.

이 문서는 이론 문서를 대신하지 않습니다.
각 토픽 레포의 이론은 해당 레포 내부 문서에서 다루고,
중앙 문서는 구현 범위와 진행 순서를 고정하는 역할만 맡습니다.

---

## 이 문서의 사용 방식

이 문서는 주차명 기준이 아니라, 학습 도메인 순서 기준으로 사용합니다.

즉, `week13`, `week14`처럼 보지 않고 아래처럼 봅니다.

- 요청/응답과 메모리 CRUD
- 영속성 저장과 계층 분리
- 안전한 요청 처리
- 인증과 JWT
- 테스트
- 캐시
- 실시간 통신
- 배포와 운영
- 자동화
- 리팩토링
- 이벤트 기반 흐름

Codex는 실습 레포를 만들기 전에 아래 순서로 문서를 참고합니다.

1. 루트 `README.md`
2. 커리큘럼 전체 운영안
3. 구현 범위 계획서
4. Codex playbook
5. 구현 문서 작성 가이드
6. 이론 문서 작성 가이드
7. 체크리스트 작성 가이드
8. 해당 도메인 순서 문서
9. 해당 레포 이론 문서 및 starter 코드

---

## 핵심 운영 원칙

### 1. 학생은 핵심 흐름만 직접 구현합니다

학생이 직접 구현하는 코드는 아래 기준으로 고릅니다.

- 그 도메인의 핵심 개념을 가장 잘 드러내는가
- 다음 단계에서도 다시 떠올려야 하는가
- 수업 시간 안에 끝낼 수 있는가
- 실행 결과를 바로 확인할 수 있는가

### 2. 아래 항목은 가급적 미리 제공합니다

- 반복적인 설정 코드
- 환경 의존성이 큰 인프라 설정
- 학습 목표와 직접 관련 없는 부가 기능
- 입문자에게 과도한 예외 상황 처리
- 긴 보일러플레이트 코드
- 복잡한 배포 스크립트 전체

### 3. 구현 문서는 결과가 아니라 순서를 보여줘야 합니다

좋지 않은 방식:

- Service를 만든다
- Controller를 만든다

좋은 방식:

1. 요청 DTO를 만든다.
2. 저장할 데이터 클래스를 만든다.
3. 저장소를 만든다.
4. Service에서 저장소를 호출한다.
5. Controller에서 Service를 연결한다.
6. Swagger 또는 테스트로 결과를 확인한다.

### 4. TODO는 순서형 힌트로 작성합니다

좋지 않은 TODO:

```kotlin
// TODO: 서비스 로직 구현
```

좋은 TODO:

```kotlin
// TODO 1. request에서 title, content 값을 꺼내세요.
// TODO 2. 이 값으로 Post 객체를 만드세요.
// TODO 3. repository.save(...)를 호출하세요.
// TODO 4. 저장 결과를 PostResponse로 변환하세요.
```

### 5. 각 도메인 문서에는 체크리스트가 반드시 들어갑니다

각 도메인 문서에는 아래 두 종류의 체크리스트가 있어야 합니다.

- 학생 체크리스트
- 강사 / 발표 / PPT 체크리스트

### 6. 한 번에 하나의 시퀀스만 작업합니다

구현 범위가 흔들리지 않도록 아래 원칙을 고정합니다.

- 현재 시퀀스 하나만 작업합니다.
- 다음 시퀀스는 사용자 승인 전 시작하지 않습니다.
- 생성 전에 이번 턴의 산출물 목록을 먼저 정리합니다.
- 작업 후에는 README 상태를 반드시 갱신합니다.

---

## 도메인 순서 맵

### 00. 선수지식 부트캠프

주제:
HTTP, JSON, Postman, Git/GitHub, DB 기초

### 01. 요청-응답과 메모리 CRUD

주제:
REST API + CRUD + Swagger

### 02. 영속성 저장과 계층 분리

주제:
JPA Basics + Layered Architecture + CRUD 확장

### 03. 안전한 요청 처리

주제:
DTO + Validation + Exception Handling

### 04. 인증과 JWT

주제:
Spring Security + JWT

### 05. 외부 인증 또는 이메일 인증

주제:
OAuth2 또는 Email Verification

### 06. 테스트와 검증

주제:
Testing Basics

### 07. 캐시와 빠른 조회

주제:
Redis Caching

### 08. 실시간 통신

주제:
WebSocket

### 09. 배포와 실행 환경

주제:
Docker + AWS Deploy Basics + 환경 분리 + 로그 확인

### 10. 자동화와 운영 흐름

주제:
CI/CD + 운영 자동화

### 11. 리팩토링과 기초 보강

주제:
종합 리팩토링 + 구조 보강 + 테스트 보강

### 12. 이벤트 기반 사고 확장

주제:
Message Queue + Event-Driven Architecture + MSA 관점 소개

---

## 각 도메인 문서 템플릿

각 `docs/sequences/*.md` 문서는 아래 형식을 따릅니다.

1. 도메인 이름
2. 이 도메인이 필요한 이유
3. 학생이 완성할 최종 흐름
4. 학생이 직접 구현할 순서
5. TODO를 넣을 파일
6. 미리 제공할 것
7. 학생 체크리스트
8. 강사 / PPT 체크리스트
9. 다음 도메인 연결 포인트

---

## 최종 한 줄 기준

좋은 구현 범위 문서는 학생이 무엇을 만들지보다,
어떤 순서로 손을 움직이면 되는지가 보이는 문서입니다.
